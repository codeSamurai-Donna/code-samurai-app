<!DOCTYPE html>
<html>
<head> <% include ./head %>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet"></head>
<body>
<% include ./header %>

    <a class="btn" href="/additem">Add New Item</a>

    <div class="card-panel pink lighten-4">


    <table class="highlight">
        <tr>

      <th>Song</th><th>Singer</th><th>Where to find</th><th>Added by</th><th>Likes</th><th>Actions</th>
        </tr>

        <% items.forEach(function(item){  %>

<tr>
    <td><%=item.song%></td>
    <td><%=item.artist%></td>
    <td><a> href="<%=item.wheretofind%>"</a><%=item.wheretofind%></td>
    <td><%=item.addedby%></td>
    <td> <i class="material-icons">check</i></td>
    <td><<i class="material-icons">cancel</i></a></td>

        </tr>

        <% }) %>
    </table>

    </div>

<% include ./footer %>
</body>
</html>
